
import CommunitySpaces from '@/components/CommunitySpaces';

const CommunitySpacesPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-50 to-blue-50">
      <CommunitySpaces />
    </div>
  );
};

export default CommunitySpacesPage;
